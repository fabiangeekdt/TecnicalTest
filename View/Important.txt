- Find out absolute paths for include.
- Find out the generic uri for the searchMovies.js file
- Improve GUI

- keep this code safe.
		//sorting the array with the movie info data
	//usort($arr_actedmovies,
	//	function ($a, $b) {
	//		$ad = new DateTime($a['releasedate']);
	//		$bd = new DateTime($b['releasedate']);
	//		
	//		if ($ad == $bd) {
	//			return 0;
	//		}
	//		
	//		return $ad < $bd ? 1 : -1;
	//	}
	//);
	
	//reverse the array so it can be watch in a descending order
	//$descending_array = array_reverse($arr_actedmovies);	